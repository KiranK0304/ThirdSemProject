$kind: http-request
method: POST
url: '{{base_url}}/auth/refresh/'
order: 3000
headers:
  - key: Content-Type
    value: application/json
body:
  type: json
  content: |-
    {
      "refresh": "{{refresh_token}}"
    }
